document:
  dsl: '1.0.0'
  namespace: test
  name: run-shell-pipeline
  version: '0.1.0'
do:
  - runShell:
      run:
        shell:
          command: echo "hello world" > /tmp/swf_test_pipeline.txt && cat /tmp/swf_test_pipeline.txt
        return: all
