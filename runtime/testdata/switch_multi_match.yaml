document:
  dsl: '1.0.0'
  namespace: test
  name: switch-multi-match
  version: '0.1.0'
do:
  - checkGrade:
      set:
        grade: '${ if .score >= 90 then "A" elif .score >= 80 then "B" elif .score >= 70 then "C" else "F" end }'
        passed: '${ .score >= 70 }'
