document:
  dsl: '1.0.0'
  namespace: test
  name: call-http-output-as-expr
  version: '0.1.0'
do:
  - getPet:
      call: http
      with:
        method: get
        endpoint:
          uri: http://localhost:9876/pets/42
      output:
        as: "${{id: .id, petName: .name}}"
