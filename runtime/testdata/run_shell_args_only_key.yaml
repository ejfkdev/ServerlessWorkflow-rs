document:
  dsl: '1.0.0'
  namespace: test
  name: run-shell-args-only-key
  version: '0.1.0'
do:
  - runShell:
      run:
        shell:
          arguments:
            Hello:
            '${.firstName}':
            '${.lastName}':
            from:
            'args!':
          command: echo
        return: all
