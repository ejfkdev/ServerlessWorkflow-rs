document:
  dsl: '1.0.0'
  namespace: test
  name: export-context
  version: '0.1.0'
do:
  - setContext:
      set:
        userName: Javierito
      export:
        as: "${ {user: .} }"
  - useContext:
      set:
        greeting: "${ \"Hello \" + $context.user.userName }"
